# bt_api_korbit

Korbit exchange package for `bt_api`.

## Installation

```bash
pip install bt_api_korbit
```

## Usage

When installed alongside `bt_api_py`, the package is discovered automatically by the unified plugin loader.

## Supported Exchanges

- KORBIT___SPOT
